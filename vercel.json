{
  "buildCommand": "npx pnpm install --no-frozen-lockfile && npx vite build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/((?!api/|schedule\.html).*)", "destination": "/index.html" }
  ]
}
